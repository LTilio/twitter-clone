import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        width: '80%',
        height: 50,
        backgroundColor: '#fff',
        borderRadius: 50,
        borderColor: '#ccc',
        borderWidth: 1,
        marginBottom: 10,
        paddingHorizontal: 10,
        paddingLeft: 20
    },
});