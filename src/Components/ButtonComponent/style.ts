import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    width: '80%',
    height: 55,
    borderRadius: 50,
    borderColor: '#00acee',
    borderWidth: 1,
    marginTop: 20,
    marginBottom: 20,
    justifyContent: 'center',
    alignItems: 'center',
  },

  text: {
    color: '#00acee',
    fontSize: 25
  },

  container:{
    backgroundColor: '#fff',
    alignItems: 'center',
    width: '100%',
    height: 'auto',
  },
});
